import { n as __exportAll } from "./chunk-nbk3hphP.js";
import { r as RegisterClass } from "./typeStore-Dy-yCYbL.js";
import { Et as isNumeric, Tt as getNumericValue } from "./lazy-Bb8psdId.js";
import { u as RichTypeAny } from "./flowGraphBlock-Rp1Fj8us.js";
import { t as FlowGraphExecutionBlock } from "./flowGraphExecutionBlock-Ddi7L2yJ.js";
//#region node_modules/@babylonjs/core/FlowGraph/Blocks/Execution/ControlFlow/flowGraphSwitchBlock.js
var flowGraphSwitchBlock_exports = /* @__PURE__ */ __exportAll({ FlowGraphSwitchBlock: () => FlowGraphSwitchBlock });
/**
* A block that executes a branch based on a selection.
*/
var FlowGraphSwitchBlock = class extends FlowGraphExecutionBlock {
	constructor(config) {
		super(config);
		this.config = config;
		/**
		* The default case to execute if no other case is found.
		*/
		this.default = this._registerSignalOutput("default");
		this._caseToOutputFlow = /* @__PURE__ */ new Map();
		this.case = this.registerDataInput("case", RichTypeAny);
		const array = this.config.cases || [];
		for (const caseValue of array) this._caseToOutputFlow.set(caseValue, this._registerSignalOutput(`out_${caseValue}`));
	}
	_execute(context, _callingSignal) {
		const selectionValue = this.case.getValue(context);
		let outputFlow;
		if (isNumeric(selectionValue)) outputFlow = this._getOutputFlowForCase(getNumericValue(selectionValue));
		else outputFlow = this._getOutputFlowForCase(selectionValue);
		if (outputFlow) outputFlow._activateSignal(context);
		else this.default._activateSignal(context);
	}
	/**
	* Adds a new case to the switch block.
	* @param newCase the new case to add.
	*/
	addCase(newCase) {
		if (this.config.cases.includes(newCase)) return;
		this.config.cases.push(newCase);
		this._caseToOutputFlow.set(newCase, this._registerSignalOutput(`out_${newCase}`));
	}
	/**
	* Removes a case from the switch block.
	* @param caseToRemove the case to remove.
	*/
	removeCase(caseToRemove) {
		if (!this.config.cases.includes(caseToRemove)) return;
		const index = this.config.cases.indexOf(caseToRemove);
		this.config.cases.splice(index, 1);
		this._caseToOutputFlow.delete(caseToRemove);
	}
	/**
	* @internal
	*/
	_getOutputFlowForCase(caseValue) {
		return this._caseToOutputFlow.get(caseValue);
	}
	/**
	* @returns class name of the block.
	*/
	getClassName() {
		return "FlowGraphSwitchBlock";
	}
	/**
	* Serialize the block to a JSON representation.
	* @param serializationObject the object to serialize to.
	*/
	serialize(serializationObject) {
		super.serialize(serializationObject);
		serializationObject.cases = this.config.cases;
	}
};
RegisterClass("FlowGraphSwitchBlock", FlowGraphSwitchBlock);
//#endregion
export { flowGraphSwitchBlock_exports as n, FlowGraphSwitchBlock as t };

//# sourceMappingURL=flowGraphSwitchBlock-BqQ7ek1m.js.map