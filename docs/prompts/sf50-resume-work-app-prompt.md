# Fresh conversation prompt: resume SF50 development

Resume SF50 work in `/Users/felg/gh/0sfs`, after the separate aircraft-selection UI task.

The objective is a source-grounded SF50 simulation with G1, original G2 and G3 choices and defensible automated comparisons to real-aircraft behavior. Do not mistake selectable variants, source-processing success or an aircraft that boots for completed calibration.

## Recover the durable context first

Read these, then inspect only the current source needed for the next implementation step:

1. `docs/validation/sf50-development-handoff.md`
2. `docs/validation/sf50-variant-models.md`
3. `docs/validation/sf50-public-data-acquisition.md`
4. `planes/Cirrus_Vision_Jet/tests/public-evidence/variant-processing-summary.json`
5. Relevant status sections of `docs/proposals/sf50-flight-model-v2.md`

Consult the expanded public-data audit, AFM audit, owner-data request and Cirrus question list when their subjects become relevant. The preceding UI task may have changed catalog/panel structure; preserve that work rather than recreating the old flat generation radio list.

No broad repository exploration is needed. Do not depend on the old conversation or temporary build directories as the only source of truth.

## Current implementation and evidence

- G1 uses the existing `cirrus-vision-jet` ID and `sf50` model. G2/G3 have separate IDs and `sf50-g2` / `sf50-g3` packages.
- The latter XML packages are generated by `scripts/build-sf50-variants.mjs` from the canonical G1 XML. Keep generation outputs synchronized when the common model changes.
- All generations still share development aerodynamics, estimated installed-engine behavior and exterior meshes. Their distinct performance has NOT been calibrated.
- Source-backed baseline replacements were applied: rated thrust 1,846 lbf, nominal usable fuel 2,001 lb, and wing MAC 62.2 in. Installed thrust, N1/TLA/bleed schedules, TSFC, loading/CG, inertia, control response and friction remain unresolved estimates.
- Latest processing verified 101 raw artifacts and emitted 820 primary AFM cruise/climb targets, 12 existing ISA runway anchors and 8,535 rows across 53 TOLD tables.
- The 820 AFM rows are allocated as 600 fitting candidates and 220 whole-condition ISA+10 same-source check rows. They are not independent validation holdouts.
- Recorder processing produced seven ERA22 and 34 public-dashboard steady-flight review candidates. These 41 windows are not yet qualified normal-flight calibration cases.
- Raw and bulk derived artifacts are locally cached but Git-ignored. A fresh clone will not automatically contain them.
- The new processor is `scripts/process-sf50-calibration-data.mjs`; full existing results are in `planes/Cirrus_Vision_Jet/tests/public-evidence/derived/variant-calibration-2026-09-12-v1/`. Prefer reading results over repeating acquisition or processing without a reason.

## Non-negotiable evidence rules

- Keep G1, G2, G2+ and G3 source applicability distinct. G2+ tables cannot silently become original-G2 or G3 calibration data.
- The historical primary G1 AFM is 31452-001 Rev 4, SHA-256 `d83e904dbd6bc3656321c93d793166d4a5c6eddf81852b15767e5c336fc23949`. Do not replace its explicit ISA entries with sparse temperature interpolation.
- The G1 sea-level takeoff source discrepancy was explained by losing the explicit ISA column. The landing revision/source discrepancy remains unresolved. Neither is a license to retune aerodynamics to contradictory targets.
- Do not borrow the later AFM takeoff procedure for the historical G1 benchmark.
- Recorded requested N1, actual N1, computed percent thrust and measured thrust are different quantities. T2 is not OAT. Bleed mass flow is not a percentage thrust penalty.
- Preserve missing values, duplicate/gap handling, source clocks and automation/safety qualification. CEN21 serial 0202 and CEN23 serial 0215 are not interchangeable with the historical G1 donor.
- The public G1 dashboard is approximately six-second processed data, not a one-second raw avionics export. Do not use it to identify fast spool, damping or flare dynamics.
- A steady trace does not prove normal operation, known weight/CG, surface inputs or brake force.
- Keep cumulative climb gallons/pounds/time/distance separate. Printed resolution is not uncertainty or a pass/fail tolerance.
- Cruise N1/TAS/fuel flow alone does not uniquely separate installed thrust from drag. Equilibrium CL inference is not measured CL-alpha.
- Do not invent a uniform 20% thrust boost or tune G3 to unmatched marketing figures.
- Public availability does not automatically authorize redistribution of AFM, owner or dashboard data.

## What to do next

Start with source qualification and the narrowest identifiable calibration step, not an arbitrary coefficient sweep.

1. Review the existing 41 steady-window candidates and the relevant primary AFM rows/conditions. Identify which quantities can be constrained now and which remain blocked by configuration, loading, channel meanings or transcription.
2. Establish a clear parameter-to-evidence plan: N1/TLA and fuel flow, installed thrust versus drag, loading/CG and pitch/control response, then takeoff/landing technique and friction. Do not use one parameter to hide an error in another subsystem.
3. Prioritize the G1 baseline, for which the strongest primary evidence exists. Keep G2/G3 explicitly provisional until their applicable primary AFM/engine configuration data supports distinct calibration.
4. Preserve the same-source check allocation and whole-flight independence boundaries before fitting. Record units, conditions, source identity, assumptions and uncertainty with each candidate.
5. Make justified changes in the owning repository and update the proposal/methodology with actual outcomes, not aspirational completion claims.

The user wants reasonable public-data avenues exhausted before approaching friends at Cirrus. Public options are not yet exhausted: normal publication access, applicable later AFMs/supplements and public-flight raw export/loading/reuse metadata remain relevant. Research those where they can resolve a specific blocker, rather than downloading unrelated material. Do not contact employees/owners, buy access or request restricted documents without permission.

## Validation status and authorization

The last actual test result was 42/42 tests across seven focused files BEFORE the latest generation/source-input changes. A prior native turbine initialization fix had four passing regressions, but the production SDK's adoption was not established. Older pilot-fitting results likewise predate the changed model.

After the most recent model changes, **only offline data processing ran**. New parser/generation tests, native/WASM flight checks and browser selection checks have not been run. Do not describe the old results as validation of current code.

This prompt does not itself authorize tests, new validation runs, a dev server or Git operations. Explain the focused checks needed and obtain explicit approval before running them. If authorized, start with the new evidence gates and generation/package selection, then matched native/WASM smoke and performance scenarios. Record exact artifact identities, conditions and the distinction between software correctness, same-source fit/checks and independent aircraft fidelity.

The native stale-N1 issue belonged in JSBSim, not an app-level cosmetic property reset. A historical SDK build exists only as a recorded temporary artifact; inspect the current dependency integration before assuming that fix is in use.

## Repository ownership

- App/UI/aircraft packages/evidence: `/Users/felg/gh/0sfs`
- Native engine: `/Users/felg/gh/Felipegalind0/jsbsim`
- WASM SDK/bindings/lifecycle/diagnostics: `/Users/felg/gh/Felipegalind0/jsbsim-wasm`
- Earth/terrain/rendering: `/Users/felg/gh/foss-earth`

Use the existing canonical repositories and ordinary branches. Do not recreate special task-named folders/worktrees, move dependency fixes into 0sfs or duplicate existing upstream PRs. Preserve user changes and tracked portability patches. PRs should follow confirmation that the relevant changes work, not precede it.

Continue implementation without unnecessary rounds of questions, but pause for genuine missing decisions or required permission. Keep updates brief, preserve the aircraft-family UI, and finish each phase by documenting what is implemented, what was actually exercised and what remains blocked or estimated.
